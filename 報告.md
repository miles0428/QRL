# QDQN on CartPole-v1 — v3 現況報告

**資料截止：2026-08-13 00:20。量子 sweep 仍在進行中（約第 350–390 / 5000 集），該節數字標為進行中。**
古典 baseline 已完成。所有數字皆為本機實測，量測條件隨數字標注。

前一份 `結論報告.md` 是 pre-v3 的紀錄，其核心結論（「這個設定實務上訓練不完」）已被 v3 解決，
請以本份為準。

---

## A0. 10 分鐘收斂需求（最新，優先於以下所有內容）

Mentor 訂的合格標準是**單次訓練在 10 分鐘內收斂**。這改變了優先順序，也讓先前
「5 seeds × 5000 集跑 4–6 小時」的做法直接出局。

### 預算分析（用 MLP 實測的 128k env steps 中位數推算）

| 設定 | 動作選擇 | 梯度 | 總計 |
|---|---|---|---|
| 每個 env step 都更新、forward 5ms | 10.7 min | 34.2 min | **44.9 min** |
| `steps_per_update=10` | 10.7 min | 3.4 min | 14.1 min |
| + forward 降到 2ms | 4.3 min | 3.4 min | **7.7 min** ✅ |

關鍵洞察：一旦採用參考實作的 `steps_per_update=10`，梯度成本就崩掉了，
**瓶頸變成每個 env step 都要跑一次的動作選擇 forward**。所以優化必須打在 batch-1 forward 上。

### 已做的兩件事

**1. 採用參考實作的訓練設定**（見 G 節對照表，這四項本來就是我們偏離的地方）：
`steps_per_update` 1→10、損失 MSE→Huber、target 同步 1→3 個梯度步、輸出加
`(⟨O⟩+1)/2` rescaling。

rescaling 的效果已證實：`w` 現在穩定在 **15–54**，不再往 500 漂。這驗證了先前的判斷 ——
那不是發散，是少了一個把 [-1,1] 映射到 [0,1] 的步驟。

**2. 模擬器優化（3.6x）**。4 qubits 的態只有 16 個複數，所以成本幾乎全是 Python dispatch
（原本每次 forward 約 820 個微小 torch 呼叫），不是算術。三項改動，都不改變計算內容：

- CX 連續段在編譯期預先合成**單一基底置換**：20 次閘作用 → 5 次 index_select
- 同軸旋轉矩陣**一次批次建構**：建一個矩陣和建 12 個都是 ~8 個 torch 操作
- 每個 qubit 的旋轉**先融合成單一 2×2** 再作用到態上：12 次態掃描 → 4 次

（`torch.compile` 是顯然的替代方案，但 Windows 上的 torch 2.2 不支援。）

| 量測（同負載） | 原始 | 最終 | 加速 |
|---|---|---|---|
| forward+backward, batch 16 | 89.2 ms | **33.1 ms** | 2.7x |
| forward only, batch 1（動作選擇） | 28.3 ms | **7.9 ms** | 3.6x |
| 實際訓練迴圈 | 14.9 ms/env-step | **5.6–6.0 ms/env-step** | 2.5x |

**3. 向量化環境（再 3.1x）**。動作選擇看似無法批次化（環境是循序的），但實測 forward
成本對 batch size **幾乎完全平坦** —— 因為是 dispatch-bound 不是算術-bound：

| batch | forward 總時間 | 每 env-step |
|---|---|---|
| 1 | 6.52 ms | 6.52 ms |
| 8 | 6.81 ms | **0.85 ms** |
| 16 | 6.80 ms | **0.43 ms** |

所以同時跑 8 個環境的成本跟跑 1 個幾乎一樣。實測 **8.96 → 2.87 ms/env-step**。

autoreset 語義是**實測**而非假設 —— 弄錯會悄悄污染 replay buffer。gymnasium 1.3 用
next-step autoreset：終止那步回傳的是真正的最終觀測（該存），但**下一步**是重置觀測、
reward 0、term=False，那筆是假象必須跳過。存進去等於教 agent
「剛倒下的狀態會通往一根全新的桿子且沒有獎勵」。

### 結果

| 版本 | 600 秒內 env steps | 最終 100 回合 greedy（5 seeds） |
|---|---|---|
| 優化前 | ~27,000 | 未達門檻 |
| + 模擬器優化 | ~67,000 | **500.0**、444.8、440.7、185.4、139.9 → **1/5 收斂** |
| + 向量化環境 | ~85,000（且 5 個 process 互相競爭） | seed0 **第 338 集 / 268 秒解出**，seed3 avg100 476.0、seed2 461.6 |

### ✅ 10 分鐘需求：已達成（seed 0，兩個標準都過）

```
seed0   ep=338   訓練 268 秒   avg100 = 476.9   FINAL100 greedy = 477.5 ± 38.2   solved=True
```

**訓練 268 秒（4.5 分鐘）收斂**，且通過最嚴格的判定 —— 最終 100 回合 greedy 評估
477.5 ≥ 475，不是靠 5 回合樣本的高點。

五個 seed 的完整結果（**兩個判定標準分開報，因為它們在兩個方向都會分歧**）：

| seed | 訓練標準 avg100≥475 | 解出集數 | env steps | greedy 100 回合 | greedy 標準 |
|---|---|---|---|---|---|
| 0 | ✅ | 339 | 67,854 | 477.5 ± 38.2 | ✅ |
| 1 | ❌ | — | — | **500.0 ± 0.0** | ✅ |
| 2 | ✅ | 524 | 127,765 | 462.7 ± 60.1 | ❌ |
| 3 | ✅ | 532 | 82,221 | **500.0 ± 0.0** | ✅ |
| 4 | ❌ | — | — | 404.6 ± 190.8 | ❌ |

**greedy 標準（CartPole-v1 的標準判定）：3/5 通過，中位數 477.5，IQR [462.7, 500.0]。
中位數 seed 通過門檻。** 訓練標準也是 3/5，但**是不同的三個**。

### 兩個標準的分歧，兩個方向都有

- **seed 2**：訓練標準過（avg100 475.1）但 greedy 只有 462.7。
- **seed 1**：訓練標準**沒過**，但最終策略是**完美的 500.0 ± 0.0**。

E 節說訓練欄位會低估 agent —— 那在 epsilon 大時成立。這裡 epsilon 已在地板 0.01，
兩者幾乎在量同一件事，分歧來自別處：

- 訓練標準要求**連續 100 集**平均 ≥ 475 才認證，所以對一個剛學會的策略反應很慢
  （seed 1 被 600 秒截斷時策略已最優，但還沒累積夠 100 集紀錄）。
- greedy 標準直接量當下的策略，但**只量一個時間點**，所以會被震盪相位影響（seed 2）。

**兩者都要報，而且要看標準差**：seed 3 與 seed 1 是 **±0.0**（真正穩定），
seed 0 是 ±38.2，seed 2 ±60.1，seed 4 ±190.8。
**標準差比平均值更能分辨「穩定解出」與「剛好抽到好相位」** ——
seed 4 平均 404.6 但 ±190.8，代表它在 500 與接近 0 之間跳。

注意：`wall_clock_s` 只計訓練時間，不含評估。最終 100 回合 greedy 評估本身要
約 325 秒（100 集 × 最多 500 步 × 6.5 ms），那是量測成本不是訓練成本。

### `w` 落在理論值上

輸出 rescaling 之後，`w` 穩定在 **104–123**。CartPole 在 γ=0.99 的 Q\* ≈ 99 ——
`w` 自己收斂到理論值附近，而不是像先前漂到 300–560。這是 G 節那個診斷最直接的證實：
先前的 `w` 不是發散，是少了 [-1,1] → [0,1] 的映射，模型只好用超大的 `w` 去補償。

### 量測方法上的注意

**週期性 5 回合評估與最終 100 回合評估不是同一個指標**，不可混用。
5 回合樣本會頻繁摸到 500，但那反映的是震盪的高點而非穩定水準；
判定收斂必須用 100 回合評估。本報告先前一版曾引用 5 回合數字作為收斂證據，已更正。

### 一個方法論上的自我修正

等價性檢查在這裡發揮了作用，但不是我預期的方向 —— 它報了 2.576 的不符，
而**被測的模擬器其實是對的**，錯的是檢查腳本自己：`forward()` 加了 rescaling 之後，
它的參考端仍在算 `raw * w`。一個會與被驗證對象脫節的參考基準，比沒有參考更糟。
現在 post-circuit 的計算收斂到單一函式 `_apply_output_head`，forward 檢查與有限差分檢查共用。
融合本身另外單獨驗證過（fast vs gate-at-a-time：4.17e-07）。

---

## A. 摘要

1. **速度問題已解決。** 上一版每個梯度步 2.16 秒，現在 16.3 毫秒（133 倍）。方法不是換框架，
   而是把 Qiskit 電路直接當成 torch 張量運算來算，讓 autograd 給精確梯度。Qiskit 仍是電路、
   observable、finite-shot 評估的權威來源。
2. **加了 greedy 評估，並立刻抓到一個訓練曲線完全看不到的問題**：策略在「完美（500）」與
   「崩潰（9–11）」之間震盪。訓練用的 `avg_reward_100` 因為對 100 集取平均，把這件事整個平滑掉了。
3. **關於 TensorFlow Quantum**：那份教學實作的正是我們在跟的同一篇論文（Skolik et al.
   2103.15084），所以它的最大價值是當**逐項規格對照表**。比對後找到四處我們偏離參考實作的地方，
   而且**四處全都是穩定性機制** —— 這與上面第 2 點的震盪現象高度吻合。詳見 G 節。
4. **已依指示建置 TFQ 實作並平行運行。** TFQ 無法使用 Qiskit 電路（實測，見 G 節），
   所以它是**第二套獨立實作**而非移植。兩套寫出相同格式的 CSV，可直接並排比較，
   用來回答唯一真正重要的問題：**震盪是我們實作的缺陷，還是這組超參數本來就這樣。**

---

## B. `results/` 裡哪些是這次的數據

判斷捷徑：**有 `_eval.json` 就代表那個 run 已經跑完**（它是訓練結束才寫的）。

### 10 分鐘需求的三輪測試（最新，看這組）

| 檔案 | 設定 | 結果 |
|---|---|---|
| `qdqn_ref10_0..4.csv` | 參考設定，優化前模擬器 | ~27k env steps / 600s，未達標 |
| `qdqn_fast10_0..4.csv` | + 模擬器優化 | ~67k env steps，**1/5 收斂**（500.0） |
| **`qdqn_vec10_0..4.csv`** | **+ 向量化環境（現行設定）** | **3/5 在 10 分鐘內解出** |

### 較早的長時間 sweep（設定已過時，保留作對照）

| 檔案 | 說明 |
|---|---|
| `qdqn_0..4.csv` | Qiskit arm，舊設定（每步更新、MSE、無 rescaling），跑到約 700/5000 集後中止 |
| `tfq_0..4.csv` | TFQ arm（Cirq，94 參數），跑到 339–753 集後暫停 |
| `mlp_baseline_0..4.csv` | 古典 5 seeds，**已完成且 5/5 全解** |
| `mlp_baseline_0..4_eval.json` | 古典最終 100 回合 greedy（全部 500.0 ± 0.0） |

判斷捷徑不變：**有 `_eval.json` 就代表那個 run 已跑完**。

三個 arm 的 CSV 欄位完全相同（已驗證），所以 `scripts/benchmark_report.py`
不需任何特判就能並排比較。**但注意**：`mlp_baseline` 的 44 個參數是對齊 Qiskit arm 的
46 個，不是對齊 TFQ arm 的 94 個 —— mlp vs tfq **不是**參數量對齊的比較。

### 不是這次的

| 檔案 | 說明 |
|---|---|
| `qdqn_prev3_0.csv` / `_final.pt` | **上一版**（qiskit-ML + SPSA）。只有 2 集、1357 秒。刻意改名保留，是舊後端 wall-clock 的唯一紀錄，速度比較要用 |
| `qdqn_smoke30_0.csv` | v3 開發途中的 30 集煙霧測試，非正式結果 |

`figures/` 內 8 張圖**全部是 pre-v3 產物**（用舊的 2 集 QDQN 和已淘汰的 300 集 MLP 畫的），
目前都是錯的，需重畫。

---

## C. 後端：為什麼是自寫 statevector

同一台機器、batch 16、同一個電路：

| 後端 | s/梯度步 | vs 2.16 s |
|---|---|---|
| qiskit-ML + SPSA（上一版，brief 的 profile 值） | 2.16 | 1× |
| qiskit-ML + parameter-shift | 更慢（每參數 2 次電路評估） | — |
| qtm（qiskit-torch-module），全核心 | **掛住**：單一 episode 十分鐘跑不完 | — |
| qtm，強制 `num_threads=1` | 1.42 | 1.5× |
| **torch_sv（本次採用）** | **0.0163** | **133×** |
| torch_sv，完整訓練迴圈內實測 | 0.0227 | 95× |

**qtm 為何失敗**：它的加速全靠 `multiprocessing.Process` 把 batch 拆給多個 worker。Windows 沒有
`fork`，只有 `spawn`，所以每次 forward/backward 都要重開直譯器、重 import torch 與 qiskit。
這是平台特性不是 qtm 的缺陷 —— 在 Linux 上它的加速如論文所述可用。依 brief 的 stop-loss 規則
（低於 5 倍就不要 debug），沒有繼續投入。

**Qiskit 仍是權威來源。** `torch_sv` 是「同一個 Qiskit 電路的更快評估器」，不是重新實作：

- 電路是 `qiskit.QuantumCircuit`，`compile_circuit` 逐個 gate **讀取** `circuit.data`，
  所以改 `build_circuit` 會自動同步，不可能偷偷不一致。
- observable 是 `SparsePauliOp`，期望值用的矩陣來自 Qiskit 自己的 `to_matrix()`，
  所以 little-endian 慣例是**繼承**來的，不是我們重推的 —— 手刻 observable 讀錯 qubit 的
  那類 bug 在這裡不可能發生。
- 振幅排序與 `qiskit.quantum_info.Statevector` 一致。
- finite-shot / 雜訊評估仍走 qiskit-aer，完全不變。

---

## D. 正確性驗證

**兩個量子 arm 都通過相同標準的驗證**，且參考實作與被測對象**零共用程式碼**。

### Qiskit arm — `scripts/verify_backend_equivalence.py`

| 檢查 | 對照基準 | 誤差 |
|---|---|---|
| forward | Qiskit `Statevector.expectation_value` | 1.3e-06（float32 精度極限） |
| 梯度 `lam`（4） | 上述 Statevector 的中央差分 | 5.6e-05 |
| 梯度 circuit（40） | 同上 | 2.3e-06 |
| 梯度 `w`（2） | 同上 | 2.9e-05 |

另外三組梯度範數與 qtm 的**獨立 adjoint 實作**對到小數點後 6 位。

### TFQ arm — `tfq/verify_against_qiskit.py`

TFQ arm 起初只有形狀與梯度非零的冒煙測試，那證明管路通，但**證明不了它在算哪個電路**。
補上的做法：在 Qiskit 裡**依教學的文字描述獨立重建**同一個電路（不是從 Cirq 物件翻譯 ——
否則 Cirq 端的錯誤會傳進它自己的參考基準），綁定相同參數後比對。

| 檢查 | 對照基準 | 誤差 |
|---|---|---|
| forward | Qiskit `Statevector.expectation_value` | 4.97e-06 |
| 梯度 `theta`（72） | 上述 Statevector 的中央差分 | 5.63e-05 |
| 梯度 `lambda`（20） | 同上 | 6.88e-06 |
| 梯度 `w`（2） | 同上 | 1.25e-06 |

這裡 Qiskit 純粹是**離線驗證工具**，不在 TFQ 訓練路徑上，訓練程式也不 import 它。

### 為什麼一定要驗梯度

**forward 相符只證明電路對。** 這個專案踩過的每一個梯度 bug（`lam` 梯度靜默為 None、
權重向量被字母排序打亂）在 forward 完全看不出來 —— 跑得起來、loss 會下降，
只是在優化一個沒人設計過的電路。

TFQ 這邊的對應風險完全相同：`ControlledPQC` 要求參數值依電路**符號排序後**的順序傳入，
`ReUploadingPQC` 用 `self.indices` 做這個排列。排錯的話每個角度都落在錯的閘上，
而 forward 一樣會跑、梯度一樣會流、loss 一樣會降。這正是 Qiskit 端字母排序造成的同一種 bug，
換個框架又出現一次 —— 所以兩邊都得驗。

---

## E. 方法學修正：greedy 評估

`episode_reward` / `avg_reward_100` 是在**帶探索**的回合上量的。epsilon=0.13 時約每 8 步就有
1 步是隨機動作，這在 CartPole 上足以讓桿子在遠不到 500 步時倒下 —— 不論策略多好。
所以訓練曲線結構性低估 agent。

實測（seed 0，第 30 集）：

| 量測方式 | reward |
|---|---|
| 訓練 `avg100`（探索中） | 33.7 |
| greedy（eps=0，同一集） | **153.2** |
| 最終 20 回合 greedy 評估 | **183.7** |

這也不只是好不好看的問題：**CartPole-v1 的標準 solve 判定本來就定義在 greedy play 上**，
用探索中的回合判定是量錯了對象。現在兩個標準都報，不互相取代。

**評估不干擾訓練，且是驗證過的而非宣稱的**：`run_greedy_rollouts` 自建環境並明確設種子，
greedy 動作選擇不抽任何隨機數，所以驅動探索與 replay 取樣的 `random` / `torch` 序列不受影響。
實測：開 `eval_every=10` 跑 30 集，訓練欄位與加 hook 之前的 CSV **逐位元相同**
（只有 `wall_clock_s` 例外，它本來就不確定）。`wall_clock_s` 也扣掉了評估時間，
否則開評估會灌水到整個後端速度比較。

---

## F. 目前結果

### ⭐ 最重要的發現：震盪不是量子模型造成的

`figures/09_greedy_eval.png`。把三個 arm 的 greedy 曲線畫在一起才看得出來 ——
**古典 MLP 也在震盪，而且震盪方式一模一樣。**

MLP seed 2 的 greedy 評估序列：

```
9  28  9  26  82  104  116  108  500  133  10  11  147  163  13  500  500
                                  ^^^            ^^  ^^            ^^^
```

500 → 133 → 10 → 11 → 147 → 163 → 13 → 500。跟量子端同一個形狀。

量化「崩潰事件」（相鄰兩個評估點之間 greedy 掉超過 200）：

| arm | 參數量 | 崩潰率 |
|---|---|---|
| MLP 古典 | 44 | 7 / 91 = **8%** |
| QDQN Qiskit | 46 | 11 / 60 = **18%** |
| QDQN TFQ | 94 | 0 / 39 = 0% |

**TFQ 那個 0% 現在還不算證據** —— 它的 greedy 值目前全在 100 以下，
「掉超過 200」在算術上不可能發生。要等它爬到高分才有意義。

**這代表什麼**：MLP 用的是**同一個 trainer、同一組 DQN 超參數**
（`target_update_every=1`、MSE 損失、每個 env step 更新一次），只有函數近似器不同。
它一樣震盪 —— 所以震盪的來源是**共用的訓練設定，不是量子模型**。

這正是 G 節四項偏離所指向的方向，而且這個證據**現在就有**，不必等 TFQ 跑完。
H 節那四項修正（rescaling / Huber / target 同步 / 更新頻率）因此優先度更高了。

一個重要的但書：**MLP 震盪但最後仍然收斂** —— 五個 seed 最後都穩定在 500，
最終 100 回合 greedy 全部 500.0 ± 0.0。所以震盪不等於學不起來，
它拖慢收斂但沒有阻止收斂。量子端能否走到同一步，要等跑完才知道。

### 古典 MLP baseline — 5/5 全解 ✅

44 個參數（VQC 是 46，對齊在 ±5% 內），透過**同一個 `trainer.train()` 迴圈**：

| seed | 解出集數 | env steps | 最終 greedy（100 回合） |
|---|---|---|---|
| 1 | 713 | 128,387 | 500.0 ± 0.0 |
| 2 | 883 | 124,157 | 500.0 ± 0.0 |
| 3 | 1,018 | 153,803 | 500.0 ± 0.0 |
| 4 | 1,125 | 125,552 | 500.0 ± 0.0 |
| 0 | 1,191 | 187,512 | 500.0 ± 0.0 |

中位數 **1,018 集 / 128,387 env steps**，IQR [883, 1125] 集。
這滿足 brief Task 4 第 2 步：古典路徑能透過同一個 trainer 解出，所以量子端若不收斂，
bug 不在 trainer。

### 量子 QDQN — 進行中，且發現策略震盪 ⚠️

greedy 評估（每 50 集、5 回合），這是本報告最重要的一張表：

| seed | ep 50 | 100 | 150 | 200 | 250 | 300 | 350 |
|---|---|---|---|---|---|---|---|
| 0 | 153 | 277 | **500** | **11** | **11** | 244 | 216 |
| 1 | 121 | 250 | 319 | 138 | **500** | 173 | 50 |
| 2 | 108 | 240 | 203 | 264 | **500** | **500** | **500** |
| 3 | 242 | 288 | 169 | 427 | 416 | 211 | — |
| 4 | **500** | 495 | 309 | **9** | 243 | 332 | **500** |

中位數 / IQR：ep 50 → 153 [121, 242]；ep 100 → 277 [250, 288]；ep 150 → 309 [203, 319]；
ep 200 → 138 [11, 264]；ep 250 → 416 [243, 500]；ep 300 → 244 [211, 332]。

**這是這份報告的核心發現。** 同期的訓練 `avg100` 是平順上升的（295 → 435），
因為它對 100 集取平均。但實際的 greedy 策略在**滿分 500 與完全崩潰 9–11 之間反覆震盪**。
seed 2 在 ep 250–350 穩定維持 500，證明這個模型**能**學到最優策略 —— 問題是留不住。

這是 DQN 的經典不穩定/災難性遺忘。若沒有加 greedy 評估，我們會從訓練曲線得出
「平順收斂中」的錯誤結論。

`w` 目前在 327–518，遠高於 Q\*≈99。原因見 G 節（我們少了一個輸出 rescaling），
不是發散 —— 因為同期 greedy 表現是能達到 500 的。

**讀這張表時的陷阱**：中位數欄看起來會誤導。第 599 集的 5-seed 中位數是 500.0、
IQR [487, 500]，看起來像收斂了；但逐 seed 看，那只是五個震盪曲線在該點的**瞬間對齊**，
下一個評估點就散開。跨 seed 聚合會把「同時處在高點」誤讀成「穩定在高點」。
判斷是否真的收斂，必須看單一 seed 的連續評估點是否**持續**維持高分
（目前只有 seed 2 在第 250–350 集做到過）。

### TFQ arm — 進行中，且梯度步數差 180 倍

| arm | 平均集數 | 平均梯度步數 |
|---|---|---|
| Qiskit（torch_sv） | 666 | **182,025** |
| TFQ（Cirq） | 328 | **1,005** |

這**不是** bug，是參考實作的設計：`steps_per_update=10` 表示每 10 個 env step 才更新一次，
加上它現在還沒學會（episode 短），所以梯度步數少了兩個數量級。
TFQ arm 目前 greedy 仍在 9–45，教學跑 2000 集，還早。

比較時要留意軸的選擇：**per episode / per env step 是強化學習該用的軸**（樣本效率），
TFQ 用更少梯度步達到同樣樣本數是它的優勢而非劣勢；但若比 wall-clock 或梯度步效率，
結論會相反。兩種軸都應該報。

---

## G. 與 TensorFlow Quantum 教學的逐項對照

參考：TFQ 官方教學 *Parametrized Quantum Circuits for Reinforcement Learning*，
其 Section 3 實作 Skolik et al. (arXiv:2103.15084) —— **與本專案跟隨的是同一篇論文**。

| 項目 | 我們（v3） | TFQ 教學（＝論文參考實作） | 影響 |
|---|---|---|---|
| 框架 / 電路庫 | PyTorch + **Qiskit** | TF/Keras + **Cirq** | 換過去等於棄用 Qiskit |
| 平台 | Windows 原生 | **無 Windows wheel**（僅 Linux manylinux2014） | 需 WSL / Docker |
| **輸出後處理** | `⟨O⟩ * w`，⟨O⟩∈[-1,1] | **`(⟨O⟩+1)/2 * w`**，先映射到 [0,1] | **直接解釋 w 跑到 327–518** |
| **損失函數** | MSE | **Huber** | 對大 TD error 穩健得多 |
| **梯度步頻率** | 每個 env step | **每 10 個 env step**（`steps_per_update=10`） | 我們多做 10 倍更新 |
| **target 同步** | 每 1 個梯度步 | 每 30 env step ≈ **每 3 個梯度步** | 我們激進 3 倍 |
| `lam` 形狀 | `n_qubits` = 4 | **`n_qubits × n_layers` = 20** | 每層 re-upload 可獨立縮放 |
| 每層每 qubit 旋轉 | RY, RZ（2 參數） | **RX, RY, RZ（3 參數）** | 見下方參數量 |
| 變分層數 | `n_layers` = 5 | **`n_layers + 1` = 6**（結尾多一層） | — |
| **可訓練參數總量** | 40 + 4 + 2 = **46** | 72 + 20 + 2 = **94**（實測自 smoke test） | 參考實作是我們的 2 倍 |
| 編碼閘 | RY(x) | **RX(x)**，且先過 `tanh` | 我們沒有 tanh 壓縮 |
| 糾纏 | CNOT ring | **CZ ring** | — |
| optimizer | Adam | Adam **`amsgrad=True`** | — |
| observables | ZZII, IIZZ | Z₀Z₁, Z₂Z₃ | 等價；動作指派相反，對稱故非 bug |
| lr（in/var/out） | 0.001 / 0.001 / 0.1 | 相同 | 已對齊 ✅ |
| gamma / batch / buffer | 0.99 / 16 / 10000 | 相同 | 已對齊 ✅ |
| epsilon（init/decay/min） | 1.0 / 0.99 / 0.01 | 相同 | 已對齊 ✅ |

### 為什麼輸出 rescaling 解釋了 w

⟨O⟩ 落在 [-1, 1]。要用 ⟨O⟩≈0.2 表示 Q≈100，`w` 就得跑到 ~500 —— 這正是我們觀察到的值。
教學先把 `(⟨O⟩+1)/2` 映射到 [0, 1]，於是 `w`≈100 就夠，而且 CartPole 的 Q 值全為正，
與 [0,1] 的值域天然吻合。我們在 [-1,1] 上乘 `w`，等於有一半值域用不到，
還把梯度尺度整個放大。

### 最重要的一點

上表**粗體的四項全部是穩定性機制**（rescaling、Huber、更新頻率、target 同步），
而我們四項全部偏離參考實作、且全部偏向更激進的方向。F 節觀察到的策略震盪與這個模式高度吻合。
這不是巧合等級的證據，但足以構成一個明確的待驗證假設。

---

## H. 建議的下一步

依預期效益排序。**每次只改一項並比較** —— 目前這輪 sweep 就是 baseline。
以下數值全部是**參考實作的值，不是我們調出來的**。

| # | 改動 | 檔案 | 理由 |
|---|---|---|---|
| 1 | 輸出改 `(⟨O⟩+1)/2 * w` | `src/models/vqc.py::VQCQFunction.forward` | 讓 `w` 回到合理尺度，縮小梯度尺度 |
| 2 | MSE → Huber | `src/trainer.py::_td_loss` | 直接針對震盪；大 TD error 不再主導 |
| 3 | target 同步改每 3 個梯度步 | `src/trainer.py` | 我們目前等於幾乎沒有 target network |
| 4 | 梯度步改每 10 個 env step | `src/trainer.py` | 同時省 10 倍算力 |
| 5 | `lam` 改 per-layer（20 個） | `vqc.py` + `torch_statevector.py` 的 encoding 索引 | 表達力，非穩定性 |
| 6 | 閘組改 RX/RY/RZ + CZ ring | `vqc.py::build_circuit` | `torch_statevector` 已支援 rx；CZ 需新增 |
| 7 | `amsgrad=True` | `scripts/train.py` | 一行 |

### 已採取的決定：建置 TFQ 實作並平行運行

已依指示完成。TFQ **無法**使用 Qiskit 電路 —— 這是實測結論不是推測：

```
circuit params: 44
QASM export FAILED: QASM2ExportError
  'Cannot represent circuits with unbound parameters in OpenQASM 2.'
after binding all params: OK
```

Qiskit 與 Cirq 之間唯一的橋是 QASM，而 QASM 2 無法表示未綁定參數。TFQ 必須讓 sympy
符號活在電路裡才能對其微分，所以參數化電路過不去。**綁定後**的電路可以匯出 ——
這代表 Qiskit 仍可作為**離線驗證工具**（把 TFQ 訓好的參數塞回 Qiskit 比對期望值），
但不在訓練路徑上。

因此 `tfq/` 是**第二套獨立實作**，不是移植。設計原則：

- **凡教學與 `src/` 不同之處，一律跟教學**，即使我們的選擇看起來更有道理 ——
  否則它就答不出它存在的目的。每一處都在原始碼標為 `DIVERGES FROM src/`。
- **CSV schema 與 `src/trainer.py` 完全相同**（已驗證），所以 `benchmark_report.py`
  零特判就能並排比較，solve 判定也統一用我們的（avg100 ≥ 475），
  而非教學的「最近 10 集平均 ≥ 500」—— 10 集窗口太短不足以稱為 solve。
- 三個 optimizer 的參數槽位**開機時斷言**而非信任。順序若變動，輸出層的 lr 0.1
  會被套到電路權重（該用 0.001），照樣訓練，只是訓練錯的東西 ——
  這正是本專案反覆踩到的 bug 形狀。

順帶修掉教學本身的一個實作 bug：`np.random.choice` 無法正確從 deque 取樣，改用索引取樣。

**這樣做的真正價值**：兩套獨立實作跑同一個問題，能區分「F 節的震盪是我們實作的缺陷」
還是「這組超參數本來就這樣」。單靠一套實作無法回答這個問題。

### 仍需留意

- TFQ 0.7.3 停在 TensorFlow 2.15 / Python 3.11 / cirq 1.3.0，**已無維護**。
- ~~TFQ arm 缺少數值驗證~~ —— **已補上**，見 D 節：`tfq/verify_against_qiskit.py`
  以獨立重建的 Qiskit 電路驗證 forward（4.97e-06）與三組梯度（最差 5.63e-05）。
  兩個 arm 現在受同一標準保護。
- 上表 1–7 項**不需要換框架**也能取得同樣收益，因為它們是超參數與架構選擇。
  若最終要保留 Qiskit arm，這仍是最省的路。

---

## I. 已知限制

- **量子 sweep 未完**（約第 350–390 / 5000 集）。F 節量子數字為進行中，跑完需重跑
  `scripts/benchmark_report.py` 更新。
- **`figures/` 01–08 全部過期**，需在 sweep 完成後重畫。
  `figures/09_greedy_eval.png` 是新畫的、資料是當前的。
  `scripts/make_figures.py` 原本有個潛在崩潰 —— v3 把 config 改成分節式後，
  它仍用 `yaml.safe_load(f)["n_qubits"]` 直接取值，會 `KeyError`；已改用
  `src.config.load_config`（唯一知道檔案結構的地方）。
- **後端速度比較**（`scripts/benchmark_backends.py`）尚未執行 —— 需要機器閒置才準，
  目前有 5 個訓練程序在跑。
- 量子端尚無 `_eval.json`，因為最終 100 回合 greedy 評估在訓練結束才跑。

---

## J. 環境

| 用途 | 環境 | 關鍵套件 |
|---|---|---|
| 訓練（本次結果） | conda `qdqn-qtm`（Windows） | qiskit 1.0.2、torch 2.2.2、qiskit-torch-module 1.0、gymnasium 1.3.0 |
| finite-shot / 速度比較基準 | conda `ntu-quantum-2026`（Windows） | qiskit 2.5.1、qiskit-machine-learning 0.9.0、torch 2.13 |
| TFQ 實驗 | WSL Ubuntu 26.04 + venv `~/tfq/.venv` | Python 3.11.15、tensorflow 2.15.0、tensorflow-quantum 0.7.3、cirq-core/google 1.3.0、sympy 1.12、numpy 1.26.4 |

兩個 Windows 環境必須分開，因為 qiskit-torch-module 綁 qiskit 1.x 而
qiskit-machine-learning 需要 2.x。TFQ 必須在 WSL，因為官方不發 Windows wheel。

### TFQ 安裝紀錄（已完成並驗證）

Ubuntu 內建 Python 3.14，但 TFQ 只支援 3.10–3.12，且 apt 沒有這些版本 ——
用 `uv` 取得 Python 3.11.15 後建 venv。

第一次 `pip install tensorflow-quantum` 卡在無限依賴回溯（下載近 1 GB 後仍未解出）。
解法是直接讀 wheel 的 METADATA 拿到真正的 pin，一次全部指定：

```
cirq-core==1.3.0   cirq-google==1.3.0   sympy==1.12   tensorflow==2.15.0（僅在 extras）
```

注意 **tensorflow 只列在 `extras` 裡**，所以單純 `pip install tensorflow-quantum`
根本不會裝 TensorFlow，必須自己指定。

冒煙測試 `~/tfq/smoke.py`（對應本專案的 `vqc.py::_smoke_test`）：
依教學結構建出 PQC，batch 8 進、`[8, 2]` 出，三組參數梯度全部非零 ——

```
circuit variational params: 72   encoding params: 20
output shape: (8, 2)
  theta   grad norm: 3.315315
  lambda  grad norm: 0.538105
  w       grad norm: 6.102180
```

重點不是「TFQ import 得起來」，而是**微分路徑端到端可用**，
以及參數量實測為 94（72 變分 + 20 lam + 2 w），是我們 46 個的兩倍。

## 重現方式

```bash
# 正確性驗證（forward + 梯度）
python scripts/verify_backend_equivalence.py --backend torch_sv --check-grad

# 單一 run
python scripts/train.py --config configs/qdqn.yaml --seed 0

# 彙整所有結果（可在 sweep 進行中執行）
python scripts/benchmark_report.py
```
