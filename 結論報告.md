# QDQN on CartPole-v1 — 結論報告

## 1. 這份報告是什麼

這次任務原本從一份較簡略的口頭需求開始，後來你提供了完整的 PDF 規格書
(`qdqn_cartpole_claude_code_prompt.md.pdf`)，內容嚴謹很多，也點名了兩個容易踩雷的
failure mode。因為兩份需求差異巨大，我依你的指示整個重寫成 `qdqn-cartpole/` 這個獨立
repo，完全照 PDF 的架構規則走。這份報告總結：做了什麼、驗證了什麼、跑出了什麼結果，
以及最重要的——**目前這個設定在實務上訓練不完，為什麼，以及有哪些優化方向可以讓它真的跑得動**。

## 2. 完成的項目

repo 結構完全照 PDF 規格：

```
qdqn-cartpole/
├── configs/{qdqn.yaml, mlp_baseline.yaml}
├── src/{models/{base,vqc,mlp}.py, replay.py, trainer.py, evaluate.py, seeds.py, plots.py}
├── scripts/{train.py, sweep_seeds.py, make_figures.py}
├── results/       # 每個 (config, seed) 一個 CSV + 最終權重 .pt
└── figures/       # 8 張圖，PNG(150dpi) + PDF
```

**硬性架構規則**：`trainer.py` 完全不 import qiskit，只透過 `nn.Module` 的
`forward()`/`parameters()`/`state_dict()` 操作模型；VQC 專屬的三組學習率
optimizer 是在 `scripts/train.py`（知道模型種類的地方）組裝好才傳進去。這個邊界在
`baseline_classical`（MLP）和 VQC 共用同一個 `trainer.train()` 迴圈的實測中被驗證過。

### 兩個規格書點名的 failure mode，都已處理

1. **輸出缺 scaling**：`⟨Z⟩∈[-1,1]`，但 CartPole 在 γ=0.99 的真實 Q 值上看到 ~100，
   沒有 trainable `w` 會直接卡在 reward~10。已加 `self.w = nn.Parameter(torch.ones(2))`，
   輸出 `raw_out * self.w`。
2. **scaling 不能放進電路**：`lam_i * x_i` 是 input 和 weight 相乘，parameter-shift
   要求參數線性進入 gate，放進電路裡梯度會悄悄壞掉。`lam`、`w` 都留在 PyTorch module，
   在 `TorchConnector` 外面。

### 意外抓到的第三個 bug（規格書沒提到，但它自己的 smoke test 設計就是為了抓這種東西）

`vqc.py` 的強制 smoke test（batch=8 隨機狀態進去，斷言三組參數 `lam`/`vqc`/`w` 的
gradient 都非 None 且非零）**第一次跑就失敗**：`lam.grad` is dead。原因是
`EstimatorQNN` 的 `input_gradients` 預設 `False`，導致 `TorchConnector` 的 backward
不會回傳對 input 的梯度，`lam` 只能透過這條路徑拿到梯度，所以整組梯度悄悄消失，
forward pass 卻完全正常、不會報錯。加上 `input_gradients=True` 後三組梯度都正常。

這正是規格書寫的「a silently dead lam gradient is the exact bug this catches」——
smoke test 抓到了，而且是我們自己踩到的第三個坑，不是照抄文件就能避開的。

### 可重現性（reproducibility）已驗證

同一個 seed 跑兩次，CSV 除了 `wall_clock_s`（本質上就是計時，本來就不該一樣）以外，
**逐欄位 byte-identical**。關鍵是 `scripts/train.py` 在建立 model **之前**先呼叫
`set_seed()`，否則 model 初始權重不會重現。

### 參數量比較

| Model | 參數量 |
|---|---|
| QDQN (VQC, 4 qubits, n_layers=5) | 46 (40 電路權重 + 4 lam + 2 w) |
| MLP baseline (4→6→2) | 44 |

差 4.3%，在規格要求的 ±20% 內。

### 古典 baseline：真的跑出結果了

`scripts/sweep_seeds.py --config configs/mlp_baseline.yaml --seeds 0 1 --max-episodes 300`
真實跑完（沒有走性能安全閥，因為 MLP 一個 gradient step 只要 ~0.01s）。結果：

- seed 0：avg_reward_100 從 ~9 一路爬到 **143.3**（第 300 集），單集最高到 377。
- seed 1：同樣的上升趨勢。
- 兩個 seed 都還沒到 475 的 solve criterion（300 集不夠，spec 預設是 2000 集），但
  上升趨勢非常清楚——證明 `trainer.py`、replay buffer、target network、epsilon 排程
  這些「model-agnostic」核心邏輯是對的。這一點很重要：**VQC 之後如果訓練速度問題解決了，
  同一套迴圈原則上就能直接用**，不用再重新驗證 trainer 本身的正確性。

已產出圖：`01_learning_curves`、`02_sample_efficiency`、`03_scaling_evolution`（QDQN 的
w/lam 初步變化）、`04_circuit_diagram`、`06_loss_epsilon`、`08_param_count_vs_performance`。
`05_q_landscape`（untrained vs trained）和 `07_rollout.gif` 需要一個真正訓練過的 QDQN
checkpoint，目前用一個有時間上限（6 分鐘）的示範跑（illustrative run，非完整收斂）產生。

## 3. 最關鍵的發現：效能安全閥觸發

規格書明確要求：「Time 100 gradient steps early and extrapolate... stop to tell me if
it exceeds ~6 hours」。`scripts/sweep_seeds.py` 已經內建這個檢查，對 `configs/qdqn.yaml`
（4 qubits, n_layers=5, 40 個可訓練電路權重）**實測**：

| 項目 | 數值 |
|---|---|
| 每個 gradient step 實測耗時 | **21.6 秒** |
| 5 seeds × 2000 episodes，worst case（每集都跑滿 500 步） | **~30,000 小時（~3.4 年）** |
| 5 seeds × 2000 episodes，樂觀估計（平均約 150 步/集，未驗證的粗估） | **~9,000 小時（~1 年）** |

兩個估計都遠遠超過 6 小時的門檻，程式**依規格正確地停下來**，沒有盲目啟動整個 sweep。

**白話結論：照規格書預設參數（5 層電路、標準 parameter-shift 梯度、qiskit-machine-learning
的 EstimatorQNN/TorchConnector 路徑），這個 QDQN 在目前這台機器上，用現在的做法，
訓練不完——不是「跑很慢」，是「跑到天荒地老都跑不完」的量級差距。** 這不是 trainer
邏輯或電路設計有錯（兩者都已用 smoke test 和 MLP 對照驗證過），純粹是**單一 gradient
step 的計算開銷**太高。

## 4. 為什麼會這麼慢：初步診斷

單獨測過一個 batch=16、n_layers=5（40 權重）的 forward+backward：forward 只要
~0.17 秒，backward（gradient）卻要 ~14.5～21.6 秒。40 個權重的 parameter-shift 需要
2×40=80 次額外電路評估，一個 4-qubit statevector 模擬本身理論上應該是微秒等級——
80 次評估耗時 20 秒，換算下來每次電路評估 ~250 毫秒，這對 4-qubit 電路來說**不合理地慢**，
強烈指向瓶頸是 **Qiskit primitive 的 Python 端 dispatch/overhead**（每次 `estimator.run()`
呼叫的 pub 建構、參數綁定、可能的 transpile 檢查），而不是真正的線性代數運算量。
換句話說：問題大概率不在「量子模擬本身很貴」，而在「呼叫量子模擬的方式很貴」。

## 5. 可優化的方向（依預期效益排序）

### 優先度高、應該先做

1. **先做 profiling，別再用猜的**。用 `cProfile` 或 `torch.profiler` 對單一
   gradient step 做剖析，確認時間到底花在哪一步（`EstimatorQNN.backward` 內的哪個
   函式、`estimator.run()` 本身、還是 parameter binding/circuit copy）。目前的診斷
   （見第 4 節）是合理推論，但還沒有 profiler 數據佐證，這應該是下一步第一件事，
   成本很低、資訊量很高。

2. **改用支援 adjoint / backprop 微分的模擬器，取代 parameter-shift**。
   Parameter-shift 需要 O(P) 次電路評估（P=可訓練參數數）；backprop-style（透過
   statevector 微分本身反傳）理論上只要 O(1)～O(2) 次，對 40 個參數來說可能是
   **一個數量級以上的加速**。這是預期效益最高的單一改動。具體選項：
   - PennyLane 的 `default.qubit` + `diff_method="adjoint"`，搭配 `qml.qnn.TorchLayer`
     取代 `TorchConnector`——介面改動不大，但需要離開 qiskit-machine-learning 的
     `EstimatorQNN` 路徑。
   - 確認 Qiskit 生態系（`qiskit_algorithms` 或未來版本的 `qiskit-machine-learning`）
     是否有原生的 adjoint/statevector-backprop gradient 可以直接插進
     `EstimatorQNN(gradient=...)`，不用整個換框架。
   - 注意：adjoint 方法通常只適用於精確模擬（無 shot noise），這符合規格書「training
     一律用 StatevectorEstimator，never shot-based」的要求，方向上沒有衝突。

3. **確認 batch 真的有攤平成單一呼叫**。規格書特別交代「Batch all 16 states into
   a single TorchConnector call. Do not loop.」——這件事本身已經做到（`forward()`
   一次丟整個 batch 進去），但值得用 profiler 確認 `EstimatorQNN` 內部沒有偷偷把
   80 次 shift × 16 筆 batch 拆成 1280 次個別呼叫，而是真的合併成少數幾次大的
   `estimator.run()`。如果目前是後者，這裡也有明顯的加速空間。

### 中等優先度、立即可用的權宜之計

4. **降低 `n_layers`**：`configs/qdqn.yaml` 目前 `n_layers=5`（40 個電路權重）。
   權重數量、進而 parameter-shift 的評估次數，大致與 `n_layers` 成正比，砍到
   2 層大約能省到 40% 左右的時間（沒解決根本瓶頸，但立即有感）。
5. **先用小規模驗證再上大 sweep**：`n_layers=1~2`、`max_episodes=100~300`、
   `--seeds 0 1`（2 個而非 5 個），確認 QDQN 真的能學起來（reward 有上升趨勢、
   `w` 真的從 1 往上爬），再決定要不要投入資源跑完整規格。
6. **平行跑多個 seed**：5 個 seed 之間互相獨立，如果本機有多核心，改用多行程
   平行跑 5 個 seed，wall-clock 最多可以除以 5——但這只解決「多 seed 的總時間」，
   不解決「單一 gradient step 太慢」的根本問題，兩者應該一起做。

### 優先度較低、值得一試但不確定效益

7. **GPU 加速的模擬器**（`qiskit_aer.AerSimulator(method='statevector', device='GPU')`）：
   如果第 4 節的診斷正確（瓶頸是 Python dispatch，不是浮點運算量），GPU 對這個
   4-qubit 電路的幫助可能有限，因為問題不在算力而在呼叫次數與呼叫開銷。建議先做
   第 1 點的 profiling 再決定要不要投入。

## 6. 建議的下一步順序

1. Profile 一個 gradient step，確認瓶頸位置（見 5.1）。
2. 依 profiling 結果決定是否切換到 adjoint-based 微分（見 5.2）——這是預期報酬
   最高的改動。
3. 改動後重新跑 `scripts/sweep_seeds.py --config configs/qdqn.yaml --seeds 0 1 2 3 4`，
   讓效能安全閥重新算一次投影時間；如果降到 6 小時以內，直接讓它跑；如果還是太慢，
   再考慮降 `n_layers` 或縮小 `max_episodes`。
4. 拿到至少 2-3 個 seed 的真實收斂資料後，重跑 `scripts/make_figures.py`，把
   `05_q_landscape`、`07_rollout.gif` 換成真正訓練收斂後的版本，而不是目前這個
   6 分鐘示範跑的版本。

## 7. 檔案位置

- 完整程式碼與設定：`qdqn-cartpole/`（`src/`、`scripts/`、`configs/`）
- 訓練結果與模型權重：`qdqn-cartpole/results/`
- 圖表：`qdqn-cartpole/figures/`
- 完整技術細節、如何執行、每個 failure mode 的處理方式：`qdqn-cartpole/README.md`
