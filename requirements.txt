# Pinned to the resolved versions in the project venv (Python 3.11.4, Windows).
# Reproduce:  python -m venv .venv && python -m pip install -r requirements.txt
# The resolved versions are also printed at the top of every run log (src/versions.py).
#
# NOTE on torch: the resolver first pulled torch 2.13.0, whose c10.dll failed to
# initialize on this Windows box (WinError 1114). Pinned to 2.6.0 (CPU), which imports
# cleanly. This is a CPU-only 4-qubit workload; no CUDA build is needed.

# --- Quantum stack ---
qiskit==1.4.6                    # pinned <2.0: qiskit-algorithms 0.3.x is not 2.0-clean
qiskit-machine-learning==0.8.4   # EstimatorQNN + TorchConnector; needs >=0.8.1 for pass_manager kwarg
qiskit-algorithms==0.3.1         # ReverseEstimatorGradient (V1 adjoint) -- reverse path
qiskit-aer==0.16.4               # finite-shot evaluation hook ONLY (never training)

# --- Deep learning / RL ---
torch==2.6.0                     # CPU; training loop, autograd, optimizer
gymnasium[classic-control]==1.3.0  # CartPole-v1 (pulls pygame-ce for render_mode="rgb_array")

# --- Numerics / data / plotting ---
numpy==2.2.6
scipy==1.15.3
matplotlib==3.11.1
pandas==3.0.5
imageio==2.37.4                  # rollout GIF
imageio-ffmpeg==0.6.0
pyyaml==6.0.3                    # config loading

# --- Key transitive pins (captured for byte-identical reproducibility) ---
sympy==1.13.1                    # required exactly by torch 2.6.0
symengine==0.13.0
rustworkx==0.18.1
